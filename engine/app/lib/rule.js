class Rule {
  constructor(name, topic, activation_value, duration) {
    this.name = name,
    this.topic = topic,
    this.activation_value = activation_value,
    this.duration = duration
  }
  
}

module.exports = Rule;
